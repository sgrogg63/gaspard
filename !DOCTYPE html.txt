<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Accueil de Pierre</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-image: url('lune-rouge.jpg');
      background-size: cover;
      background-position: center;
      color: white;
      text-align: center;
    }
    .clock {
      font-size: 3em;
      margin-top: 20px;
    }
    .quote {
      font-style: italic;
      margin: 20px;
    }
    .weather {
      margin: 20px;
    }
    .search {
      margin: 20px;
    }
    .shortcuts a {
      margin: 10px;
      color: white;
      text-decoration: none;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="clock" id="clock"></div>
  <div class="quote" id="quote">"Chaque jour est une nouvelle chance."</div>
  <div class="weather" id="weather">Météo à Royat : chargement...</div>
  <div class="search">
    <form action="https://www.google.com/search" method="get">
      <input type="text" name="q" placeholder="Recherche Google" />
      <button type="submit">🔍</button>
    </form>
  </div>
  <div class="shortcuts">
    <a href="https://www.francebleu.fr" target="_blank">Radio</a>
    <a href="https://www.meteo.fr" target="_blank">Météo</a>
    <a href="https://www.qwant.com" target="_blank">Qwant</a>
  </div>

  <script>
    function updateClock() {
      const now = new Date();
      const time = now.toLocaleTimeString('fr-FR');
      document.getElementById('clock').textContent = time;
    }
    setInterval(updateClock, 1000);
    updateClock();

    fetch("https://api.open-meteo.com/v1/forecast?latitude=45.77&longitude=3.05&current_weather=true")
      .then(response => response.json())
      .then(data => {
        const temp = data.current_weather.temperature;
        const wind = data.current_weather.windspeed;
        document.getElementById("weather").textContent = `Météo à Royat : ${temp}°C, vent ${wind} km/h`;
      })
      .catch(() => {
        document.getElementById("weather").textContent = "Météo à Royat : erreur de chargement.";
      });
  </script>
</body>
</html>

